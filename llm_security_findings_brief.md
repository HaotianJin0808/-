# 大模型安全漏洞情报三条重点摘录（可发客户）

## 1) GitHub Copilot 遭被动提示注入，可能导致代码仓库被接管

- **核心风险**：攻击者可通过“被动提示注入”影响 Copilot 输出/行为，进而触发对代码仓库的高危操作。  
- **可能影响**：代码泄露、恶意代码引入、开发链路被污染（供应链风险）。  
- **客户侧建议**：
  1. 关闭或限制高权限自动提交/自动执行；
  2. 对 AI 生成代码启用强制审查与差异检测；
  3. 对关键仓库启用分支保护和最小权限 token。

## 2) SAST + AI 漏洞修复（检测到治理）

- **核心信息**：这条更偏“防守能力建设”，不是单一漏洞通告，而是强调 AI 在漏洞修复流程中的落地。  
- **风险视角**：如果企业过度依赖自动修复而缺少人工验证，可能引入“误修复”或新缺陷。  
- **客户侧建议**：
  1. 把 AI 修复放在“建议层”，最终合入要有人审；
  2. 增加回归测试和安全测试门禁；
  3. 对修复结果做可追溯审计。

## 3) 芯片漏洞可能导致 AI 模型数据泄露（苹果/高通/AMD）

- **核心风险**：底层硬件/微架构漏洞可跨越应用层防护，影响模型推理数据与敏感信息。  
- **可能影响**：模型输入输出、推理中间态、敏感业务数据泄露风险上升。  
- **客户侧建议**：
  1. 及时跟进厂商补丁与固件升级；
  2. 关键模型推理环境做隔离（租户/进程/节点）；
  3. 对高敏场景启用更严格的数据最小化与脱敏。
